include <stdio.h>

/**
 * add - addition
 * @a: number
 * @b: number
 * Return: result
 */
int add(int a, int b)
{
	return (a + b);
}

/**
 * sub - substitution
 * @a: number
 * @b: number
 * Return: result
 */
int sub(int a, int b)
{
	return (a - b);
}

/**
 * mul - multipication
 * @a: number
 * @b: number
 * Return: result
 */
int mul(int a, int b)
{
	return (a * b);
}

/**
 * div - division
 * @a: number
 * @b: number
 * Return: result
 */
int div(int a, int b)
{
	return (a / b);
}
int mod(int a, int b)
{
	return (a % b);
}
