#include "main.h"

/**
 * print_square - function that print a square
 * @size: input integer
 * Return: Always 0
 */
void print_square(int size)
{
	int i, j;

	if (size > 0)
	{
		for (i = 0; < size; i++)
		{
			for (j = o; j < size; j++)
				_putchar('0');
			_putchar('\n');
		}
	}
	else
		_putchar('\n');
}
